# PR-02 Bundle Comparison

This comparison uses raw filesystem bytes from `dist/assets`, sorted by size and then filename. The baseline is commit `6b75286` (the merged PR-01 mainline), and the after report is this PR's production build. Full inventories are available in the adjacent `pr-02-bundle-before.txt` and `pr-02-bundle-after.txt` files.

| Measure | Before | After | Change |
|---|---:|---:|---:|
| Modern entry | 2,398.64 KiB | 323.11 KiB | -2,075.53 KiB |
| Legacy entry | 2,762.40 KiB | 458.57 KiB | -2,303.83 KiB (-83.39%) |
| Legacy 3,000 KiB budget headroom | 237.60 KiB | 2,541.43 KiB | +2,303.83 KiB |

> The post-change build passes the unchanged 3,000 KiB legacy hard gate. Its largest legacy JavaScript asset is `github.min-legacy-mA468bxD.js` at 996.19 KiB; the legacy entry is 458.57 KiB.

The report is generated by `npm run bundle:report` after a production build. It identifies modern and legacy entrypoints, lists every JavaScript chunk with raw KiB, and ranks the largest load units. The report does not alter runtime behavior or the existing size-gate script.
